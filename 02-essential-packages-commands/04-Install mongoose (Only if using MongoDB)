# What it does:

Connects Node.js backend to MongoDB using schemas and models.

## Syntax:
```js
npm install mongoose
```
## Steps:

- Run: npm install mongoose

Use:

const mongoose = require("mongoose");

Result:
"dependencies": {
  "mongoose": "^8.x.x"
}
